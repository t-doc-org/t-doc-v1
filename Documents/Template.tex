\documentclass[a4paper,11pt]{article}
\usepackage{commonpackages}
% import des packages nécessaires qui ne sont pas dans le fichier commonpackages.sty

\begin{document}
\title{Noter le titre ici}
\date{}
\maketitle
% Rédaction du document en LaTeX avec les balises personnalisées suivantes:
% Video Youtube: \video[taille en % optionnelle, par défaut 50\%]{référence Youtube de la vidéo}
% Fenêtre Geogebra: \geogabra[taille en % optionnelle, par défaut 100\%]{référence du document geogebra}
% Image: \includegraphics[width=0.8\textwidth]{images/NomImage.png} Il est obligatoire de définir la largeur en fonction de la largeur du texte.
% Code source: \begin{code}{python} ... \end{code} Il faut indiquer le langage. La syntaxe du code doit être correcte.
% Code exécutable: \begin{code}[interactive]{python} ... \end{code} Seul le langage python peut être exécuté.
% Code turtle exécutable: \begin{code}[interactive_turtle]{python} ... \end{code}
% Solution: \begin{solution} ... end{solution}

\end{document}
